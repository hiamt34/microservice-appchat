apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-server-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-server
  template:
    metadata:
      labels:
        app: user-server
    spec:
      containers:
        - name: user-server
          image: hiamt34/user-server-app-chat
          env:
            - name: PORT_AUTH_SEVER
              value: "1001"
            - name: HOST_AUTH_SEVER
              value: "auth-server-clusterip-srv"
            - name: PORT_MES_SEVER
              value: "1004"
            - name: HOST_MES_SEVER
              value: "mes-server-clusterip-srv"
            - name: PORT_ROOM_SEVER
              value: "1003"
            - name: HOST_ROOM_SEVER
              value: "room-server-clusterip-srv"
            - name: PORT_USER_SEVER
              value: "1002"
            - name: HOST_USER_SEVER
              value: "0.0.0.0"
            - name: DB_URI
              value: "mongodb+srv://d17dccn195:HGzmf4T3cRgC3Pqp@chat-microservice.mnefmvv.mongodb.net"
            - name: SERVERNAME
              value: "user-server"
          ports:
            - containerPort: 1002
          volumeMounts:
            - name: log
              mountPath: /usr/src/app/logs
        - name: filebeat
          image: elastic/filebeat:7.16.3
          args:
            - -c
            - /etc/filebeat/conf.yaml
            - -e
          volumeMounts:
            - name: filebeat-config
              mountPath: /etc/filebeat
            - name: log
              mountPath: /var/log
      volumes:
        - name: log
          emptyDir: {}
        - name: filebeat-config
          configMap:
            name: filebeat-config

---
apiVersion: v1
kind: Service
metadata:
  name: user-server-clusterip-srv
spec:
  type: ClusterIP
  selector:
    app: user-server
  ports:
    - name: user-server-clusterip
      protocol: TCP
      port: 1002
      targetPort: 1002
